#include "main.h"
#include <stdio.h>

/**
 * main - outputs the main
 * @argc: this is the argument count
 * @argv: The argument vector
 *
 * Return: Always 0 (success)
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
