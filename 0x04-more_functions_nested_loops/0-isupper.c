#include "main.c"
/**
* _issuper: -checks if a number is super
* x - is the number to be chaecked
* Return: 1 for super and 0 otherwise
*/
int _issuper(int x)
{
if(x >= 65 && x <= 90)
{
return (1);
}
return (0);
}
